{
  "name": "loop-shift-website",
  "version": "2.2.7",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "node scripts/serve.mjs",
    "web": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "db:generate": "drizzle-kit generate"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10",
    "drizzle-orm": "0.45.2"
  },
  "dependencies": {
    "@libsql/client": "0.18.0"
  }
}
